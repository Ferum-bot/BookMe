package com.levit.book_me.ui.activities.creating_profile

/**
 * Needed to show/hide title and page indicator
 * of {CreatingProfileActivity} from fragments
 */
interface TitleViewController {

    fun showTitle(show: Boolean)
}